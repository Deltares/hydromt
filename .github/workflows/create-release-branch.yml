name: Create release branch (minor/major)

# Creates `release/vX.Y` from main at X.Y.0 (version, changelog, switcher).
# Main is NOT bumped here; the record-release PR does that after the release.

on:
  workflow_dispatch:
    inputs:
      bump:
        type: choice
        description: "Is this a Minor or Major release?"
        options:
          - minor
          - major
        default: minor

defaults:
  run:
    shell: bash -l -e {0}

jobs:
  create-release-branch:
    name: Create release branch and PR
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    concurrency:
      group: NEW_RELEASE
      cancel-in-progress: false
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: main

      - name: Compute new version and branch
        id: version
        run: |
          # For minor: use main's current version. For major: bump major, reset minor.
          CURRENT_VERSION=$(bash .github/scripts/get-version.sh \
            | sed -E 's/^([0-9]+\.[0-9]+\.[0-9]+).*$/\1/')
          MAJOR=$(echo "$CURRENT_VERSION" | cut -d'.' -f 1)
          MINOR=$(echo "$CURRENT_VERSION" | cut -d'.' -f 2)

          case "${{ inputs.bump }}" in
            minor)
              NEW_MAJOR=$MAJOR
              NEW_MINOR=$MINOR
              ;;
            major)
              NEW_MAJOR=$((MAJOR + 1))
              NEW_MINOR=0
              ;;
          esac

          NEW_VERSION="$NEW_MAJOR.$NEW_MINOR.0"
          RELEASE_BRANCH="release/v$NEW_MAJOR.$NEW_MINOR"

          if git ls-remote --exit-code --heads origin "$RELEASE_BRANCH" >/dev/null; then
            echo "Release branch '$RELEASE_BRANCH' already exists on origin."
            exit 1
          fi

          echo "NEW_VERSION=$NEW_VERSION" >> "$GITHUB_OUTPUT"
          echo "RELEASE_BRANCH=$RELEASE_BRANCH" >> "$GITHUB_OUTPUT"

      - name: Create release branch with version bump
        env:
          NEW_VERSION: ${{ steps.version.outputs.NEW_VERSION }}
          RELEASE_BRANCH: ${{ steps.version.outputs.RELEASE_BRANCH }}
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "<>"

          git checkout -b "$RELEASE_BRANCH"
          git push --set-upstream origin "$RELEASE_BRANCH"

          # Bump version, update changelog & switcher, commit, push.
          bash .github/scripts/setup-release.sh "$NEW_VERSION"
