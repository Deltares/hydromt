---
name: Refresh CI cache
on:
  push:
  # workflow_dispatch:
  # pull_request:
  #   types:
  #     - closed

jobs:
  refresh-cache:
    runs-on: ubuntu-latest
    # if: github.event.pull_request.merged == true || github.event == 'workflow_dispatch'
    steps:
      - name: Check out code
        uses: actions/checkout@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: generate env file
        run: make full-environment.yml
      - name: docker hub lgoin
        uses: docker/login-action@v2
        with:
          username: savente
          password: dckr_pat_2qn2zUadS4Paoshkq35odcHmYJQ
      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          target: deps-only
          tags: savente/hydromt-deps-only
          cache-from: type=gha
          cache-to: type=gha,mode=max
