
.. _migration:

Migrating to v1
---------------

- Support for using `.ini` files has been removed. From now on HydroMT only supports configuration using files in the YAML format. Any config `.ini` files you have should be converted to their equivalent `.yaml` format.
