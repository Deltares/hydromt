# note that these are the developer dependencies,
# if you only want the user dependencies, see
# dependencies in pyproj.toml
name: hydromt-dev

channels:
  - conda-forge

dependencies:
  - affine
  - black
  - bottleneck
  - cartopy # to run examples
  - click
  - dask
  - descartes # to run examples
  - entrypoints
  - flit>=3.4
  - gdal>=3.1
  - geopandas>=0.10
  - jupyter # to run examples
  - matplotlib # to run examples
  - nbsphinx # docs notebook examples
  - netcdf4
  - numpy
  - openpyxl
  - pandas
  - packaging
  - pcraster # optional
  - pip
  - pydata-sphinx-theme  # docs
  - pyflwdir>=0.5.4
  - pygeos>=0.8 # optional dependency for mesh
  - pyproj
  - pytest # tests
  - pytest-cov # tests
  - pytest-mock # tests
  - python>=3.7
  - rasterio
  - responses # tests
  - requests
  - rioxarray
  - scipy
  - testpath  # tests
  - toml
  - shapely
  - sphinx # docs
  - xarray
  - xugrid>=0.1.5 # optional for mesh
  - zarr
  - pip:
    - hydroengine  # optional
    - sphinx_design  # docs
    - sphinx_autosummary_accessors  # docs

